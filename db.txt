CREATE DATABASE provida_club;
USE provida_club;
-- drop table users;
-- CREATE TABLE users (
--   id INT AUTO_INCREMENT PRIMARY KEY,
--   username VARCHAR(50) UNIQUE NOT NULL,
--   password_hash VARCHAR(255) NOT NULL
-- );

-- INSERT INTO users(username, password)
-- VALUES ('admin', 'admin@123');

CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(100) NOT NULL
);

INSERT INTO users(username, password)
VALUES ('admin', 'admin@123');

USE provida_club;

CREATE TABLE competitions (
  id INT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(150) NOT NULL,
  description TEXT NULL,
  rules TEXT NULL,
  prize TEXT NULL,
  price DECIMAL(10,2) NULL,        -- optional
  currency VARCHAR(10) NULL,       -- optional (USD/KHR)
  is_open TINYINT(1) NOT NULL DEFAULT 1,
  event_date DATE NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

select * from competition_media;
CREATE TABLE competition_media (
  id INT AUTO_INCREMENT PRIMARY KEY,
  competition_id INT NOT NULL,
  media_type ENUM('IMAGE','VIDEO') NOT NULL,
  media_url VARCHAR(255) NOT NULL,     -- can be file path or YouTube link
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (competition_id) REFERENCES competitions(id) ON DELETE CASCADE
);



USE provida_club;


















SELECT * FROM competitions;

SELECT id, title FROM competitions WHERE is_open='1' ORDER BY id DESC ;

SELECT * FROM registrations;
-- --42
SELECT * FROM competition_media;
SELECT * FROM competitions;

CREATE TABLE IF NOT EXISTS registrations (
  id INT AUTO_INCREMENT PRIMARY KEY,
  competition_id INT NOT NULL,
  full_name VARCHAR(150) NOT NULL,
  phone VARCHAR(50) NOT NULL,
  email VARCHAR(150) NULL,
  note TEXT NULL,
  proof_image_url VARCHAR(500) NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_reg_competition_id (competition_id),
  CONSTRAINT fk_reg_competition
    FOREIGN KEY (competition_id) REFERENCES competitions(id)
    ON DELETE CASCADE
);





-- --uploads/news/7/images/img_69ada5209adfc4.23258444.jpg
-- uploads/7/images/featured_69ac59afa13486.51913273.jpg
SELECT * FROM news_media;

use provida_club;
SELECT * FROM news_media;
-- 1) TABLES (MySQL)
CREATE TABLE IF NOT EXISTS news_posts (
  id            INT AUTO_INCREMENT PRIMARY KEY,
  title         VARCHAR(255) NOT NULL,
  excerpt       TEXT NULL,
  content       LONGTEXT NULL,
  news_date     DATE NULL,
  is_published  TINYINT(1) NOT NULL DEFAULT 1,
  created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS news_media (
  id         INT AUTO_INCREMENT PRIMARY KEY,
  news_id    INT NOT NULL,
  media_url  VARCHAR(512) NOT NULL,        -- ex: /images/news/news_12_1.jpg
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT fk_news_media_news
    FOREIGN KEY (news_id) REFERENCES news_posts(id)
    ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE INDEX idx_news_posts_date ON news_posts (news_date);
CREATE INDEX idx_news_posts_pub  ON news_posts (is_published);
CREATE INDEX idx_news_media_news ON news_media (news_id);




ALTER TABLE news_posts
ADD COLUMN slug VARCHAR(255) UNIQUE NULL,
ADD COLUMN subtitle VARCHAR(255) NULL,
ADD COLUMN category VARCHAR(120) NULL,
ADD COLUMN tags VARCHAR(255) NULL,
ADD COLUMN author_name VARCHAR(120) NULL,
ADD COLUMN featured_image VARCHAR(255) NULL,
ADD COLUMN video_url VARCHAR(255) NULL,
ADD COLUMN external_video_url VARCHAR(255) NULL,
ADD COLUMN meta_title VARCHAR(255) NULL,
ADD COLUMN meta_description TEXT NULL,
ADD COLUMN allow_comments TINYINT(1) NOT NULL DEFAULT 0,
ADD COLUMN view_count INT NOT NULL DEFAULT 0;
CREATE TABLE news_media (
    id INT AUTO_INCREMENT PRIMARY KEY,
    news_id INT NOT NULL,
    media_type ENUM('image','video') NOT NULL,
    media_url VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_news_media_post
        FOREIGN KEY (news_id) REFERENCES news_posts(id)
        ON DELETE CASCADE
);
    SELECT * FROM news_posts;
    
    SELECT * FROM news_media;

ALTER TABLE news_media
ADD COLUMN sort_order INT NOT NULL DEFAULT 0;

select * from news_media;

SELECT * FROM news_posts;
SELECT * FROM news_gallery;

SELECT * FROM news_media;

CREATE TABLE news_gallery (
    id INT AUTO_INCREMENT PRIMARY KEY,
    news_id INT NOT NULL,
    image_path VARCHAR(255) NOT NULL,
    sort_order INT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_news_gallery_news
        FOREIGN KEY (news_id) REFERENCES news_posts(id)
        ON DELETE CASCADE
);
	CREATE TABLE news_posts (
		id INT AUTO_INCREMENT PRIMARY KEY,
		title VARCHAR(255) NOT NULL,
		subtitle VARCHAR(255) DEFAULT NULL,
		author_name VARCHAR(150) DEFAULT 'Admin',
		news_date DATE DEFAULT NULL,
		excerpt TEXT DEFAULT NULL,
		content LONGTEXT DEFAULT NULL,
		featured_image VARCHAR(255) DEFAULT NULL,
		body_image VARCHAR(255) DEFAULT NULL,
		external_video_url VARCHAR(500) DEFAULT NULL,
		is_published TINYINT(1) NOT NULL DEFAULT 0,
		view_count INT NOT NULL DEFAULT 0,
		created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
		updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
	);

	CREATE TABLE IF NOT EXISTS news_media (
		id INT AUTO_INCREMENT PRIMARY KEY,
		news_id INT NOT NULL,
		media_type ENUM('image','video') NOT NULL DEFAULT 'image',
		media_url VARCHAR(255) NOT NULL,
		thumbnail_url VARCHAR(255) NULL,
		alt_text VARCHAR(255) NULL,
		sort_order INT NOT NULL DEFAULT 0,
		created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
		CONSTRAINT fk_news_media_news
			FOREIGN KEY (news_id) REFERENCES news_posts(id)
			ON DELETE CASCADE
	);
    
    
    
    
    use provida_club;
    
    CREATE TABLE site_settings (
    id INT AUTO_INCREMENT PRIMARY KEY,

    about_text_en TEXT NULL,
    about_text_kh TEXT NULL,

    hero_title_en VARCHAR(255) NULL,
    hero_title_kh VARCHAR(255) NULL,

    hero_subtitle_en VARCHAR(255) NULL,
    hero_subtitle_kh VARCHAR(255) NULL,

    hero_description_en TEXT NULL,
    hero_description_kh TEXT NULL,

    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);



INSERT INTO site_settings (
    about_text_en,
    about_text_kh,
    hero_title_en,
    hero_title_kh,
    hero_subtitle_en,
    hero_subtitle_kh,
    hero_description_en,
    hero_description_kh
) VALUES (
    'Provida Pétanque Club is dedicated to promoting the sport of pétanque in Cambodia. The club brings together players of all ages and backgrounds to learn, practice, and enjoy the game while building a strong and friendly community.',
    'ក្លឹប Provida Pétanque ផ្តោតលើការលើកស្ទួយកីឡាប៉េតង់នៅកម្ពុជា។ ក្លឹបនេះប្រមូលផ្តុំអ្នកលេងគ្រប់វ័យ និងគ្រប់មជ្ឈដ្ឋានឱ្យបានរៀន អនុវត្ត និងរីករាយជាមួយការលេង ខណៈពេលកសាងសហគមន៍ដែលរឹងមាំ និងរួសរាយរាក់ទាក់។',
    'Welcome to Provida Pétanque Club',
    'សូមស្វាគមន៍មកកាន់ក្លឹប Provida Pétanque',
    'Cambodia’s Pétanque Community',
    'សហគមន៍ប៉េតង់របស់កម្ពុជា',
    'Master the sport of pétanque in a growing community of players and competitions.',
    'អភិវឌ្ឍជំនាញកីឡាប៉េតង់របស់អ្នកក្នុងសហគមន៍អ្នកលេង និងការប្រកួតដែលកំពុងរីកចម្រើន។'
);